<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/activity_date_picker_dialog"
    android:layout_width="300dp"
    android:layout_height="400dp"
    tools:context="com.team.codealmanac.w2do.dialog.DatePickerDialogActivity"
    android:orientation="vertical">

    <android.support.v4.view.ViewPager
        android:id="@+id/act_date_picker_dialog_viewpager"
        android:layout_width="match_parent"
        android:layout_height="350dp"/>
    <Button
        android:id="@+id/act_date_picker_dialog_okButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="end"
        android:background="#00000000"
        android:textColor="@color/blue_grey"
        android:text="OK"/>
</LinearLayout>
